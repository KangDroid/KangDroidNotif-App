package com.kangdroid.notification.settings

class Settings {
    companion object {
        var mDisableChargingNotification: Boolean = false
        var mHashBlackList: HashMap<String, Boolean> = HashMap()
    }
}